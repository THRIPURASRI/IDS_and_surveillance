# config.py
import os

class Config:
    SECRET_KEY =
    SQLALCHEMY_DATABASE_URI =
    SQLALCHEMY_TRACK_MODIFICATIONS =

    # Email (SMTP) Settings
    SMTP_SERVER =
    SMTP_PORT = int(587)
    SMTP_USER =
    SMTP_PASSWORD =

    # Twilio (SMS) Settings
    TWILIO_ACCOUNT_SID =
    TWILIO_AUTH_TOKEN =
    TWILIO_FROM_NUMBER =
